# ShortURL Frontend

Minimal React frontend (Vite) for the ShortURL API.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Open the provided localhost URL and use the form to POST to `/shorten`.
